export const validateField = (data) => {
    var error = null;
    if(data.name===null){
        error = 
    }
    
    console.log(data);
    return 'error'
    // if(validator)
    // switch (field) {
    //     case 'name':
    //         if (validator.isEmpty(value)) {
    //             error = 'Name is required';
    //         }
    //         break;
    //     case 'email':
    //         if (!validator.isEmail(value)) {
    //             error = 'Invalid email address';
    //         }
    //         break;
    //     case 'phoneNo':
    //         if (!validator.isLength(value, { min: 7 }) && !validator.isNumeric(value)) {
    //             error = 'Phone Number must be at least 6 digits and cannot contain characters';
    //         }
    //         break;
    //     default:
    //         error = '';
    //         break;
    // }

    // return error;
};

